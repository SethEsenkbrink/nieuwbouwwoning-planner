# Sessie 06 — 1 augustus 2026 · Het woningdossier

**Doel:** blok E1 en E2 uit het bouwplan — het woningpaspoort en het onderdelenregister.
**Uitkomst:** E1, E2 én E3 af (plus E5 en E6, die met E3 meekwamen), ADR-0013 en ADR-0014,
een `CLAUDE.md`, en een dode verify-gate die zes sessies lang niets controleerde.

---

## 0. Wat de scan opleverde

De sessie begon met het doorlezen van alle documentatie en een scan van de codebase
(~15.000 regels TS/TSX, 15 routes, 19 componenten, 20 lib-modules). Vier dingen klopten niet:

| Bevinding | Ernst |
| --- | --- |
| `docs/STATE.md` en het sessielog van 05 stonden ongecommit — juist de wijzigingen waarin STATE.md meldt dat de werktree schoon is | Verwarrend bij het overnemen |
| `PROJECT.md` §8 gaf TypeScript 7.x, terwijl ADR-0003 die versie expliciet verwierp en `package.json` op 6.x staat | De "vaste waarheid" was fout |
| `PROJECT.md` §5 had de gebreken-velden per abuis onder `nabudget` geplakt | Kopieerfout |
| `model.ts` miste `NabudgetpostDoc` in de aliaslijst | Inconsistentie |

Alle vier hersteld en gecommit als `3eed5c5`.

### De git-lock

Committen lukte eerst niet: `.git/index.lock`, 0 bytes, van 31 juli 23:24 — twaalf uur oud en
zonder draaiend git-proces. Dat is de crash ná commit `7b80825`, en dezelfde soort blokkade
als in sessie 01.

`rm` gaf *Operation not permitted*: de FUSE-mount van de werkmap blokkeert delete standaard.
Na expliciete toestemming ging het wel. Staat nu als valkuil in `STATE.md`.

## 1. Het onderzoek dat het model veranderde

Seth gaf de installaties door: **NIBE warmtepomp**, **Brink WTW**, zonnepanelen met omvormer,
een waterontharder, een drinkwaterfilter, en waarschijnlijk een **plug-and-play thuisbatterij**.
Verzoek erbij: maak de bibliotheek breed, met meerdere merken en typen, en laat mensen specs
kunnen invullen.

Het onderzoek naar die markt leverde vier dingen op die ADR-0010 niet dekte — en één daarvan
was niet voorzien.

### Merk en type zijn niet genoeg

Bij een storing zoek je niet naar "NIBE" maar naar het vermogen, het koudemiddel of de SCOP.
Die velden verschillen volledig per categorie: een omvormer heeft MPPT-ingangen, een batterij
een cyclusgarantie, een WTW-unit een filterklasse. Vaste kolommen zouden dertig lege velden
opleveren of een notitieveld waar niets uit te halen valt.

Opgelost met `specs: Record<string, string>` plus een **voorgestelde veldenlijst per categorie**
uit de bibliotheek. Een map telt in de rules als één veld, dus de documentlimiet blijft heel.

### Plug-and-play is juridisch iets anders dan vast geïnstalleerd

Dit kwam binnen als terloopse opmerking en bleek de scherpste scheidslijn in het register:

| | Plug-and-play | Vast geïnstalleerd |
| --- | --- | --- |
| Juridisch | roerende zaak | nagelvast, onroerend |
| Bij verkoop | verhuist mee | blijft achter |
| Verzekering | inboedel | opstal |
| Installateur | geen | erkend installateur |

Zonder dat onderscheid genereert het overdrachtsdossier (E8) straks een lijst met apparatuur
die de verkoper heeft meegenomen. Twee velden: `montage` en `blijftBijWoning` — **bewust los
van elkaar**, want de regel kent uitzonderingen in beide richtingen.

### De vondst die niemand had aangevraagd

Sinds **7 mei 2024** moet elke thuisbatterij vanaf **0,8 kW** die terug kan leveren worden
aangemeld bij de netbeheerder via Energieleveren.nl — **ook een plug-and-play model**, en
vrijwel elke stekkerbatterij zit boven die grens. Zonder melding mag de netbeheerder je
teruglevering weigeren of je aansluiting beperken.

Dat is exact het soort termijn waar deze app voor bestaat, en het zou zonde zijn als het
register de gegevens wél kent maar de verplichting niet. Het is een veld op het onderdeel
geworden (`registratieplicht`) dat werkt zoals `gecommuniceerdeDatum` bij afspraken: zolang
`aangemeldOp` leeg is, staat het onderdeel op de actielijst.

### Het energielabel is een klok, geen tekstje

Een energielabel is **tien jaar geldig vanaf de opnamedatum**, heeft een registratie in
EP-online, en verloopt stil: is het verlopen, dan is het ook uit EP-online en MijnOverheid
verdwenen — en bij verkoop heb je een geldig label nodig. Dezelfde mechaniek als de
garantieklokken uit blok D.

Alle vier vastgelegd in **ADR-0013**, plus de keuze om het woningpaspoort als geneste map op
te slaan in plaats van als losse velden.

## 2. E1 — het woningpaspoort

- `woningStatus` (`in_aanbouw` / `opgeleverd`) en `woningpaspoort` op het project
- `lib/woning.ts`: `woningStatusVan()`, `isOpgeleverd()`, `bepaalEnergielabelstand()`,
  `paspoortstand()`, `adresregel()` — 20 tests
- `/woning` met de fase, de energielabelklok en het paspoort in drie blokken
- Het dashboard wisselt van kop en toont een verlopen energielabel bovenaan
- Rules: `geldigWoningdossier()` + `geldigPaspoort()` met een eigen groottecheck op de map

**Het migratiepad is een expliciete keuze.** Een project zonder `woningStatus` geldt als
`in_aanbouw`, met een test die dat vastlegt. Zou de standaard `opgeleverd` zijn, dan kregen
alle bestaande projecten ineens de onderhoudslijst op een woning die nog niet bestaat.

Gecommit als `a6a46de`.

## 3. E2 — het onderdelenregister

`data/onderdelen-standaard.ts` bevat **17 onderdelen** met per stuk: waarom je het vastlegt,
de montagevorm, de gangbare garantietermijn, gangbare merken, bekende typereeksen en de
specvelden die ertoe doen. Seths installaties zitten er allemaal in, inclusief beide
batterijvormen.

De merken staan als **datalist-suggesties** en niet als keuzelijst: `merk` en `type` blijven
vrije strings in het model, en er is bewust géén `verify:rules`-koppeling op merknamen. Een
lijst die veroudert mag geen harde regel worden, en een ontbrekend merk mag de gebruiker nooit
blokkeren.

`lib/onderdelen.ts` rekent: `berekenGarantieklok()`, `registratieOpenstaand()`,
`garantiesDieAflopen()`, `telOverdracht()`, `sorteerOnderdelen()`, `ordenSpecs()` — 23 tests.
De sortering zet een openstaande meldplicht boven een aflopende garantie, want die eerste heeft
een consequentie bij de netbeheerder.

Het dashboard toont nu openstaande registraties en garanties die binnen 90 dagen aflopen.

Gecommit als `0093810`.

## 4. De verificatiestap vond het grootste probleem van de sessie

Aan het eind is een subagent losgelaten op de nieuwe code, met de opdracht datumberekeningen
na te rekenen, pariteit te controleren en te zoeken naar paden die de validatie omzeilen.
Die vond, buiten blok E om, dit:

> **`npm run typecheck` heeft nooit iets gecontroleerd.**

Het script stond op `tsc --noEmit`, maar `tsconfig.json` bevat `"files": []` met project
references naar `tsconfig.app.json` en `tsconfig.node.json`. In die opzet doet `tsc --noEmit`
niets en geeft hij **exit 0**. De typecheck-stap in `npm run verify` was sinds sessie 01 een
lege huls — en dat betekent dat de TypeScript van blok A tot en met E nooit door een poort is
gegaan.

Nagerekend in plaats van aangenomen, door een harde typefout in te voegen:

| Gate | Uitkomst met `const kapot: number = "tekst"` |
| --- | --- |
| `tsc --noEmit` (oud) | exit **0** — fout niet gezien |
| `tsc --build --force` (nieuw) | exit **2** — TS2322 op de juiste regel |

Zodra de echte gate aanstond kwamen er twee bestaande fouten uit, allebei van vóór blok E:

- **TS2379 × 2** op `lib/projecten.ts:130` en `:163`. `ProjectInvoer` (waar `undefined` een
  geldige waarde is) is onder `exactOptionalPropertyTypes` niet toewijsbaar aan
  `Partial<ProjectData>`. Opgelost met een gedeeld `Invoer<T>`-type in `converters.ts`, met
  uitleg waarom `Partial` hier niet volstaat.
- **TS5101** — `baseUrl` is deprecated in TS 6 en verdwijnt in 7. Verwijderd; `paths` werkt
  zonder.

**De les:** een verify-stap die altijd slaagt, ziet eruit als een verify-stap die werkt. Net
als bij `request.resource.size()` in sessie 03 was de enige manier om het te vinden een test
die er expliciet op mikte.

### Wat de subagent verder vond

Drie echte bevindingen in de nieuwe code, alle drie hersteld:

1. **`Woning.tsx` legde stilzwijgend "tussenwoning" vast.** De keuzelijst had geen lege optie
   en `woningtype` ging onvoorwaardelijk mee bij het opslaan. Wie het paspoort invulde zonder
   die lijst aan te raken, kreeg een woningtype als feit — en `paspoortstand()` telde het
   daarna als ingevuld. Nu een "Nog niet gekozen"-optie, net als bij het energielabel.
2. **`Onderdelen.tsx` liet een decimale garantietermijn door.** `Number.isFinite` accepteert
   `60.5`, maar de rule eist `is int`. Gevolg zou een permissiefout zijn die niets zegt over
   de oorzaak. Nu `Number.isInteger` met een passende melding.
3. **De nieuwe converters hadden geen unit-tests**, terwijl elke andere converter er wel een
   heeft. 18 tests bijgekomen op de paspoort- en onderdeelconverters, inclusief de terugvallen
   (`"overig"`, `"nvt"`, `blijftBijWoning: true`) en de afkap van een te grote specs-map.

Ook gemeld en bewust **niet** veranderd: de rules kennen nergens `keys().hasOnly(...)`, dus een
document mag onbekende veldnamen bevatten zolang het totaal onder de limiet blijft. Dat geldt
voor álle collecties sinds sessie 01, dus het is een bestaande keuze en geen regressie van blok
E. Het verdient een eigen afweging in een latere sessie.

## 5. Wat er is gecontroleerd — en wat niet

**Groen in de sandbox:**

| Check | Uitkomst |
| --- | --- |
| `tsc --build --force` | ✅ — de échte typecheck, zie §4 |
| `verify:tokens` | ✅ 50 tokens |
| `verify:headers` | ✅ 10 headers, 14 CSP-directives |
| `verify:rules` | ✅ 24 enums / 119 waarden (was 19 / 81) |

Het `verify:rules`-script is uitgebreid: de hardgecodeerde `ankerTypes()`-afhandeling is
vervangen door een lijst hulpfuncties, zodat `woningtypes()`, `energielabels()` en
`onderdeelCategorieen()` er automatisch bij horen. **Negatief getest op twee scenario's** —
een waarde uit de rules halen en een hulpfunctie hernoemen — en beide keren faalde het script
met een bruikbare melding.

**Lokaal gedraaid door Seth, om 12:36 — alles groen:**

| Check | Uitkomst |
| --- | --- |
| `npm run verify` | typecheck · lint · **283 tests in 16 bestanden** · tokens · headers · rules-pariteit · build ✅ |
| `npm run rules:test` | **116 tests** ✅ in 7,8 s |
| Build | 161 modules, `App` 260 kB (69 kB gzip), `firebase` 567 kB (167 kB gzip) |

Lint gaf vier fouten, alle vier in de nieuwe code en alle vier hersteld in `e79c83b`:

- `onderdelen.ts:85` — `prefer-optional-chain`: `regel.klok !== null && regel.klok.bijnaVoorbij`
  werd `regel.klok?.bijnaVoorbij === true`.
- `onderdelen.ts:165` en `:170` — `non-nullable-type-assertion-style` op de `as string` in
  `ordenSpecs`. De regel stelt een `!` voor, maar dat onderdrukt `noUncheckedIndexedAccess`
  in plaats van hem op te lossen. Herschreven naar `flatMap` (die de undefined-check en de
  waarde in één stap doet) en `Object.entries` (die de waarde meelevert). Nul assertions.
- `Dashboard.tsx:207` — `prefer-nullish-coalescing` op `(opgeleverd && adres) || project.naam`.
  Werd een ternary, want `??` werkt niet op de `false` die `&&` kan opleveren.

Bij het narekenen of hetzelfde patroon elders zat, kwam er nog een vijfde uit die de linter
niet zag: in `Onderdelen.tsx` stond `{(installateur ?? onderdeel.installatieDatum) && …}` —
dat mengt een betrokkene met een datum, en bij een onderdeel mét installateur maar zónder
installatiedatum begon de regel met " · ". Nu opgebouwd uit de delen die er zijn.

**Dat er na zes sessies met een dode typecheck-gate maar vier lintfouten uitkwamen, is
opmerkelijk** — en het zegt iets nuttigs: de lintstap zelf heeft altijd gewerkt. Alleen de
typecheck stond open, en `eslint` met type-aware regels vangt een deel van hetzelfde net.
De twee TS2379's in `projecten.ts` vielen precies in het gat ertussen.

## 6. Twee dingen om te onthouden

**Een geneste map telt in de rules als één veld.** `withinSize(25)` beschermt de inhoud van
`woningpaspoort`, `specs` en `registratieplicht` dus niet. Elke map heeft nu een eigen
`.size()`-check, met tests die bewijzen dat een opgeblazen map wordt geweigerd. Zonder die
checks is zo'n map een vrij beschrijfbare opslagbak en sneuvelt constraint C2 in stilte —
precies dezelfde vorm als het `.data`-gat uit sessie 03.

**`MetDatums<T>` is niet recursief.** Een `Timestamp` in een geneste map blijft een
`Timestamp`. Bij `ProjectData` en `OnderdeelData` is dat met een expliciete `Omit` opgelost,
met een comment erbij — een slim mapped type dat het stilzwijgend goed doet zou bij de derde
geneste map alsnog fout gaan, en dan ver van de oorzaak.

## Bestanden

**Nieuw:** `docs/decisions/ADR-0013-…md` · `src/lib/woning.ts` + test ·
`src/lib/onderdelen.ts` + test · `src/data/woning-opties.ts` ·
`src/data/onderdelen-standaard.ts` · `src/routes/Woning.tsx` · `src/routes/Onderdelen.tsx` ·
dit sessielog

**Gewijzigd:** `src/types/model.ts` · `src/lib/converters.ts` + test · `src/lib/projecten.ts` ·
`src/routes/Dashboard.tsx` · `src/App.tsx` · `src/data/navigatie.ts` ·
`src/lib/navigatie.test.ts` · `firebase/firestore.rules` · `firebase/rules.test.ts` ·
`scripts/verify-rules.mjs` · `package.json` (typecheck-script) · `tsconfig.app.json` ·
`docs/PROJECT.md` · `docs/STATE.md` · `docs/decisions/README.md`

## Voor de volgende sessie

Blok **E3 — het onderhoudsschema**. Dat is de enige echte modeluitbreiding uit ADR-0010 §2:
`intervalDagen` + `laatstUitgevoerdOp`, met `berekenVolgendeOnderhoud()` in de rekenkern.
Een deel van het voorwerk staat al in `data/onderdelen-standaard.ts` — de filterintervallen
van de WTW, het RO-membraan en de anode van de boiler zijn daar als spec-hint genoteerd en
kunnen naar de onderhoudsbibliotheek verhuizen.

Aandachtspunt voor de bundel: `App-*.js` groeide deze sessie van 209 naar 260 kB. Elk nieuw
scherm zit in dezelfde chunk, en de onderdelenbibliotheek met alle merken en specvelden zit nu
in de bundle van iedereen die inlogt — ook wie nog midden in de bouw zit. Route-based code
splitting is na blok E waarschijnlijk meer waard dan het opsplitsen van de firebase-chunk.

---

# Deel 2 — blok E3, en de omgeving

## 7. Eerst de omgeving doorgelicht

Op verzoek van Seth is de werkomgeving nagelopen op negen punten (CLI-tooling, MCP-servers,
rechten, mounts, runtimes, Docker, mock-services, testautomatisering, context). In plaats van
te theoretiseren is er gemeten. De uitkomst weersprak de aanname achter de vraag:

**"Mijn werkomgeving" bestaat niet — het zijn er twee.** De AI-sandbox (Ubuntu, 2 cores,
3,8 GB, geen root, weg na de sessie) en Seths Windows-machine, met de projectmap als
FUSE-brug ertussen. Daarom draaien vitest en eslint hier niet: `node_modules` op de mount
bevat `@rolldown/binding-win32-x64-msvc`.

Getest of een Linux-install dat oplost:

| Poging | Uitkomst |
| --- | --- |
| `npm ci` in een aparte map | Faalt — de lockfile is platformspecifiek (`Missing: @emnapi/runtime`) |
| `npm install` idem | **7+ minuten zonder één geschreven pakket** |
| Node 24 installeren | nodejs.org geeft HTTP 403 via de proxy-allowlist |

Conclusie: de werkverdeling — de AI bouwt en typecheckt, Seth draait `npm run verify` — is
niet een gebrek maar de snelste opzet die er is. Van de negen punten bleek er één echt iets
op te leveren: **`CLAUDE.md` in de projectroot** (commit `97ac304`), met de sandbox-grenzen,
de geverifieerde versies en de `tsc --noEmit`-val. De rest was al aanwezig, geblokkeerd door
bewuste grenzen, of loste een probleem op dat dit project niet heeft.

Bijvangst: Seths versies zijn geverifieerd en er is **geen enkel conflict** — Node 24.12.0
(eis ≥24), Temurin 21.0.12 (emulator-eis 21+), `JAVA_HOME` correct.

## 8. Blok E3 — het onderhoudsschema

Drie ontwerpvragen vooraf voorgelegd; alle drie beantwoord en vastgelegd in **ADR-0014**:

1. **Seizoenswerk** → `voorkeursmaand` op de taak, die de berekende datum naar de
   *dichtstbijzijnde* voorkomen van die maand verschuift. Dakgoten blijven zo in het najaar,
   ook als je ze een keer in maart doet.
2. **Het logboek** → meteen mee in plaats van in E6. Afvinken schrijft `laatstUitgevoerdOp`
   én een logregel in één `writeBatch`. Zonder dat overschrijft elke beurt de vorige, en die
   historie is niet te reconstrueren.
3. **De herinneringen** → ADR-0010 §4 maakte e-mail een *voorwaarde* voor blok E, maar dat
   botst met het besluit om live gaan uit te stellen tot ronde 8. **ADR-0014 §3 herziet dat
   gedeeltelijk:** de onderhoudslijst komt op het dashboard, de mailfunctie volgt in ronde 8.
   Het risico — een dossier dat zich niet meldt wordt misschien niet gebruikt — is niet
   weerlegd maar geaccepteerd, mét een toets om achteraf te zien of dat terecht was.

**Opgeleverd:** `lib/onderhoud.ts` (35 tests), een bibliotheek van 30 taken waarvan 9 met een
voorkeursmaand, twee subcollecties met rules, `/onderhoud` met afvinken en historie, en de
onderhoudslijst op het dashboard.

## 9. De verificatiepass vond opnieuw iets echts

**`voorkeursmaand` kon naar het verleden schuiven.** Bij een interval korter dan een jaar:

> Interval 182 dagen, voorkeursmaand oktober, laatst gedaan op 15 oktober → berekend 15 april
> → kandidaten zijn oktober vorig jaar (182 dagen terug) en oktober dit jaar (183 vooruit) →
> de vroegste wint → **15 oktober, de dag van de beurt zelf.**

De taak was dan meteen achterstallig en bleef dat: elke keer afvinken leverde dezelfde datum
op. Bij interval 30 liep de reeks zelfs een dag per beurt achteruit. Opgelost met een
`nietVoor`-ondergrens in `naarMaand()`, twee regressietests, en een waarschuwing in het
formulier zodra iemand een voorkeursmaand combineert met een interval onder de 300 dagen.

De standaardbibliotheek raakte dit niet — alle negen taken met een voorkeursmaand hebben een
interval van een jaar of meer — maar het formulier en de rules stonden de combinatie vrij toe.

**En een belofte die door niets werd afgedwongen.** Het commentaar boven het rules-blok zei
dat er geen volgende-datum in een onderhoudstaak mag staan, maar er was geen test én geen
rule die dat tegenhield: de rules begrenzen alleen het *aantal* velden. `onderhoudstaken` is
nu de enige collectie met `keys().hasOnly(...)`, met drie tests erop. Voor de andere
collecties blijft dit een openstaand punt, genoteerd in `STATE.md`.

Verder: twee dode exports verwijderd, en "1 dagen over tijd" vervangen door helpers die het
meervoud goed doen.

## 10. Stand na deze sessie

| | Begin | Eind |
| --- | --- | --- |
| Unit tests | 220 | **318** |
| Rules-tests | 79 | **141** |
| ADR's | 12 | **14** |
| Rules-pariteit | 19 enums / 81 waarden | **25 / 121** |
| Schermen achter login | 10 | **13** |

Nog te draaien door Seth: `npm run verify` en `npm run rules:test`. Dat tweede is niet
optioneel — er zijn twee collecties bijgekomen en `onderhoudstaken` heeft een gesloten
veldenlijst die bij een vergeten veld élke write weigert.
